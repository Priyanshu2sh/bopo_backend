services:
  web:
    build: .
    command: /bin/sh -c "python3 manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p 8000 bopo_backend.asgi:application"
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - .:/app 